---
title: "MDX サンプル記事"
date: "2025-10-31"
---

# こんにちは MDX

これは MDX で書かれたサンプル記事です。`pages/blog/` に `.mdx` を追加すると、自動でルーティングされます。

## コード例

```js
export const greet = (name) => `Hello, ${name}!`
```

任意で React コンポーネントも書けます。

<button onClick={() => alert('クリックありがとうございます!')}>クリック</button>

