timeout: 1200s
options:
  logging: CLOUD_LOGGING_ONLY
substitutions:
  _REGION: asia-northeast1
  _AR_REPO: myblog
steps:
  # 1) Build container image
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/myblog:${BUILD_ID}
      - .

  # 2) Push image to Artifact Registry
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/myblog:${BUILD_ID}

  # 3) Create a Cloud Deploy release (uses skaffold in this repo)
  - name: gcr.io/cloud-builders/gcloud
    args:
      - deploy
      - releases
      - create
      - release-${BUILD_ID}
      - --delivery-pipeline=myblog-pipeline
      - --region=${_REGION}
      - --skaffold-file=skaffold.yaml
      - --images=myblog=${_REGION}-docker.pkg.dev/$PROJECT_ID/${_AR_REPO}/myblog:${BUILD_ID}
      - --source=.
